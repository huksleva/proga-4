[project]
name = "Комп практикум ЛР14"
version = "0.1.0"
description = "Предмет: 'Компьютерный практикум'. Лабораторная работа №14"
readme = "README.md"
requires-python = ">=3.13.2"

dependencies = [
    "beautifulsoup4",
    "requests",
    "pandas"
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
